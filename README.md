# 🏠 Real Estate Agency Database

Современное веб-приложение для управления и просмотра недвижимости с интерактивной картой.

![Real Estate Agency](https://img.shields.io/badge/Flask-3.0.0-blue)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Latest-blue)
![Python](https://img.shields.io/badge/Python-3.8+-green)

## ✨ Особенности

- 🔍 Поиск недвижимости по адресу
- 🏘️ Фильтрация по типу (Квартира, Дом, Участок)
- 🗺️ Интерактивная карта с маркерами (Leaflet)
- 📱 Адаптивный дизайн
- 🔒 Защита от SQL-инъекций (параметризованные запросы)
- 🎨 Современный UI/UX
- 📊 API endpoint для получения данных в JSON

## 📁 Структура проекта

```
real-estate-db/
├── app.py                 # Основное Flask приложение
├── config/
│   └── config.py          # Конфигурация приложения
├── templates/
│   ├── index.html         # Главная страница
│   └── error.html         # Страница ошибок
├── static/
│   ├── css/
│   │   └── style.css      # Стили
│   └── js/
│       └── map.js         # JavaScript утилиты
├── requirements.txt       # Python зависимости
├── .env.example          # Пример переменных окружения
├── .gitignore            # Git ignore файл
├── real_estate_agency.sql # SQL дамп базы данных
└── README.md             # Документация
```

## 🚀 Быстрый старт

> 💡 **Самый простой способ:** Используйте `./run.sh` или `docker-compose up`  
> Подробнее в [QUICKSTART.md](QUICKSTART.md)

### Вариант 1: Использование Docker (рекомендуется)

```bash
# Запустить всё одной командой (БД + приложение)
docker-compose up

# Или в фоновом режиме
docker-compose up -d

# Остановить
docker-compose down
```

Приложение будет доступно по адресу: http://localhost:5000

### Вариант 2: Использование скрипта запуска

```bash
chmod +x run.sh
./run.sh
```

### Вариант 3: Ручной запуск

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd real-estate-db
   ```

2. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте базу данных**
   ```bash
   # Создайте базу данных PostgreSQL
   createdb real_estate_agency
   
   # Восстановите дамп (если у вас есть pg_restore)
   pg_restore -d real_estate_agency real_estate_agency.sql
   ```

4. **Настройте переменные окружения**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл с вашими настройками БД
   ```

5. **Запустите приложение**
   ```bash
   python app.py
   ```

6. **Откройте в браузере**
   ```
   http://localhost:5000
   ```

## ⚙️ Конфигурация

Создайте файл `.env` на основе `.env.example`:

```env
DATABASE_URL=postgresql://user:password@localhost/real_estate_agency
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=your-secret-key-here
```

## 🗄️ База данных

База данных PostgreSQL должна содержать таблицу `property` со следующими полями:
- `property_id` - ID недвижимости
- `type` - Тип (Apartment, House, Land)
- `address` - Адрес
- `price` - Цена
- `bedrooms` - Количество спален
- `latitude` - Широта
- `longitude` - Долгота

## 📡 API

### GET /api/properties

Возвращает список недвижимости в формате JSON.

**Параметры запроса:**
- `search` - Поиск по адресу
- `type` - Фильтр по типу

**Пример:**
```bash
curl http://localhost:5000/api/properties?type=Apartment
```

## 🛠️ Технологии

- **Backend:** Flask 3.0.0
- **Database:** PostgreSQL (psycopg2)
- **Frontend:** HTML5, CSS3, JavaScript
- **Maps:** Leaflet.js
- **Styling:** Modern CSS с CSS Variables

## 🔧 Решение проблем

Если возникают проблемы с установкой зависимостей (особенно на Python 3.13+), см. [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

## 🔒 Безопасность

- ✅ Параметризованные SQL-запросы (защита от SQL-инъекций)
- ✅ Валидация входных данных
- ✅ Обработка ошибок
- ✅ Конфигурация через переменные окружения

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Приветствуются любые улучшения! Создавайте issues и pull requests.

---

**Сделано с ❤️ для управления недвижимостью**
