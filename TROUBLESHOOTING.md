# 🔧 Решение проблем

## Проблема: Ошибка установки psycopg2-binary на Python 3.13

### Симптомы
```
Error: pg_config executable not found.
pg_config is required to build psycopg2 from source.
```

### Решения

#### Решение 1: Установить PostgreSQL development headers (рекомендуется)

**macOS:**
```bash
brew install postgresql
```

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install libpq-dev python3-dev
```

**Затем запустите снова:**
```bash
./run.sh
```

#### Решение 2: Использовать альтернативный установщик

```bash
./install.sh
```

#### Решение 3: Ручная установка

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите pip и инструменты сборки
pip install --upgrade pip setuptools wheel

# Установите с отключенной изоляцией сборки
pip install --no-build-isolation psycopg2-binary

# Установите остальные зависимости
pip install Flask python-dotenv
```

#### Решение 4: Использовать Docker (самый простой способ)

```bash
docker-compose up
```

Docker автоматически настроит всё необходимое.

## Проблема: База данных не подключается

### Ошибка: "role 'postgres' does not exist"

**Причина:** В Homebrew PostgreSQL используется ваше системное имя пользователя, а не `postgres`.

**Решение:**
```bash
# Автоматическое исправление (рекомендуется)
./start-db.sh
# Скрипт автоматически обновит .env файл

# Или вручную обновите .env:
# Замените postgresql://postgres:password@ на postgresql://ваше_имя@
```

### Проверьте:

1. **PostgreSQL запущен:**
   ```bash
   # macOS
   brew services list
   # или
   pg_isready
   
   # Linux
   sudo systemctl status postgresql
   ```

2. **Правильные настройки в .env:**
   ```env
   # Для Homebrew PostgreSQL (macOS)
   DATABASE_URL=postgresql://ваше_имя@localhost/real_estate_agency
   
   # Для стандартной установки
   DATABASE_URL=postgresql://postgres:password@localhost/real_estate_agency
   ```

3. **База данных существует:**
   ```bash
   # Автоматически
   ./setup-db.sh
   
   # Или вручную
   createdb real_estate_agency
   ```

4. **Восстановлен дамп:**
   ```bash
   # Автоматически (попробует восстановить из real_estate_agency.sql или init-db.sql)
   ./setup-db.sh
   
   # Или вручную
   pg_restore -d real_estate_agency real_estate_agency.sql
   # Или используйте init-db.sql как fallback
   psql -d real_estate_agency -f init-db.sql
   ```

## Проблема: Модуль не найден

Если видите `ModuleNotFoundError`:

1. Убедитесь, что виртуальное окружение активировано:
   ```bash
   source venv/bin/activate
   ```

2. Переустановите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

## Проблема: Порт 5000 занят

Измените порт в `app.py`:
```python
app.run(host='0.0.0.0', port=5001, debug=app.config['FLASK_DEBUG'])
```

Или убейте процесс, использующий порт:
```bash
# macOS/Linux
lsof -ti:5000 | xargs kill -9
```

## Дополнительная помощь

Если проблема не решена:
1. Проверьте версию Python: `python3 --version`
2. Проверьте версию pip: `pip --version`
3. Убедитесь, что все системные зависимости установлены
4. Попробуйте использовать Docker для изоляции окружения

