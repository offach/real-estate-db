# 🚀 Быстрый старт

## Самый простой способ запуска

### 1. Используя Docker Compose (самый простой - рекомендуется)

```bash
# Запустить всё одной командой (БД + приложение)
docker-compose up
```

Приложение будет доступно по адресу: http://localhost:5000

### 2. Используя скрипт запуска

**Шаг 1: Настройте базу данных**
```bash
# Запустить PostgreSQL и автоматически создать базу
./start-db.sh

# Или полная настройка (пересоздание БД с данными)
./setup-db.sh
```

> 💡 **Важно:** Скрипты автоматически определяют ваше имя пользователя и обновляют `.env` файл. В Homebrew PostgreSQL используется ваше системное имя пользователя, а не `postgres`.

**Шаг 2: Запустите приложение**
```bash
./run.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит зависимости
- Проверит подключение к БД
- Запустит приложение (автоматически найдет свободный порт, если 5000 занят)

### 3. Ручной запуск

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Настройте .env файл
cp .env.example .env
# Отредактируйте .env с вашими настройками

# 3. Запустите приложение
python app.py
```

## Требования

- Python 3.8+
- PostgreSQL 12+
- pip

## Проблемы?

### Порт 5000 занят (macOS)
Приложение автоматически найдет следующий свободный порт. Или отключите AirPlay Receiver:
- System Preferences → General → AirDrop & Handoff → AirPlay Receiver

### PostgreSQL не запущен
```bash
# macOS
./start-db.sh
# или
brew services start postgresql

# Или используйте Docker (проще всего)
docker-compose up db
```

### Другие проблемы
См. [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

