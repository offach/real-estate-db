# 🚀 Быстрый старт

## Самый простой способ запуска

### 1. Используя скрипт запуска (рекомендуется)

```bash
./run.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит зависимости
- Проверит подключение к БД
- Запустит приложение

### 2. Используя Docker Compose

```bash
# Запустить всё одной командой
docker-compose up

# Или в фоновом режиме
docker-compose up -d
```

Приложение будет доступно по адресу: http://localhost:5000

### 3. Ручной запуск

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Настройте .env файл
cp .env.example .env
# Отредактируйте .env с вашими настройками

# 3. Запустите приложение
python app.py
```

## Требования

- Python 3.8+
- PostgreSQL 12+
- pip

## Проблемы?

Если возникают проблемы с подключением к БД:
1. Убедитесь, что PostgreSQL запущен
2. Проверьте настройки в `.env` файле
3. Убедитесь, что база данных `real_estate_agency` существует

